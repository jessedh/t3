# Legacy Facet Documentation

Specifications and analyses for facets that have been removed from the active
manifest. Retained for audit/historical evidence only. None of these facets are
deployed or present in `scripts/lib/facet-manifest.js`. See
`archive/legacy-facets/README.md` for the facet replacement table and
`archive/legacy-facets/` for the archived source.

| Document | Subject Facet | Status | Replaced By |
|---|---|---|---|
| `SecureSettle_Specification.md` | SecureSettleFacet | Archived (Tier-0 A.3 — ghost execution on external assets; never safe to deploy) | Wave 5 settlement engine (`SettlementCycleFacet`) |
| `SecureSettle_Comprehensive_Analysis.md` | SecureSettleFacet | Archived (same) | Wave 5 settlement engine (`SettlementCycleFacet`) |

Do not use these documents as current implementation guidance.
